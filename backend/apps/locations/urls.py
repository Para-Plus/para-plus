"""
URLs pour les locations de matériel.
"""
from django.urls import path

app_name = 'locations'

urlpatterns = [
    # À implémenter: créer location, vérifier disponibilité, liste locations, etc.
]
